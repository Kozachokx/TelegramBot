const UNITS = {
  STANDART: 'standard', 
  METRIC: 'metric',
  IMPERIAL: 'imperial'
}

const WEATHER_PERIOD = {
  NOW: 'NOW',
  DAY: 'DAY',
  WEEK: 'WEEK',
}

module.exports = { UNITS, WEATHER_PERIOD }
